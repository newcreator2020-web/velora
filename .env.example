# Project
NODE_ENV=development
TZ=UTC

# -----------------------------------------------------------------------------
# Supabase (cloud project: dgekfjkuvnofwdwxflms)
# -----------------------------------------------------------------------------
# Public — safe to bundle in the browser (anon key).
NEXT_PUBLIC_SUPABASE_URL=https://dgekfjkuvnofwdwxflms.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SERVERSIDE ONLY — NEVER EXPOSE TO BROWSER / GIT / LOGS.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_ID=dgekfjkuvnofwdwxflms

# Direct Postgres password (for `supabase link` / `psql` style utilities; optional).
SUPABASE_DB_PASSWORD=

# -----------------------------------------------------------------------------
# Reserved future env vars (placeholders; intentionally empty until used).
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
