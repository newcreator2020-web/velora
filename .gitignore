# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage
playwright-report
test-results

# next.js
.next
out
build
next-env.d.ts

# production
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# ide
.vscode
.idea
*.swp
*.swo

# vitest
coverage/

# playwright artifacts (ignore browser binaries + output dirs only; keep tracked specs remain tracked)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/.playwright-browsers/
e2e/.cache/
e2e/test-results/
e2e/storageState*.json
e2e/storage-state*.json
e2e/*-state*.json

# Explicit ignore for scratch WIP only (tracked specs: fase6/7/8/9 + global-setup stay tracked; do not commit loose untracked WIP drafts
e2e/scratch/
e2e/drafts/
# If a loose untracked scratch spec must be ignored, list it explicitly below instead of blanket /e2e/

# IDE / AI agent tool-local files (NOT project governance).
# Project technical rules are the single source of truth in TRAE Project Rules
# and the docs/ folder. Do not commit regenerated scratch files.
AGENTS.md
CLAUDE.md
CLAUDE_todo*.md

# Supabase CLI sandbox home workaround (FASE 4 Windows EPERM)
.supabase-home/
supabase/.home/
