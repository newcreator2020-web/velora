{
  "framework": "nextjs",
  "buildCommand": "pnpm build || next build",
  "installCommand": "pnpm install --prefer-offline --no-frozen-lockfile --ignore-scripts ; pnpm rebuild esbuild",
  "outputDirectory": ".next"
}
